{
  "name": "gate88",
  "version": "1.0.0",
  "description": "Gate 88 - HTML5/TypeScript port of the 2005 game by Jonathan Mak",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx tsc --noEmit && npx vite build",
    "preview": "vite preview",
    "typecheck": "npx tsc --noEmit",
    "typecheck:server": "npx tsc --project tsconfig.server.json --noEmit",
    "lan:server": "tsx server/lanServer.ts",
    "dev:lan": "concurrently \"npm run dev\" \"npm run lan:server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.8"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.20.0"
  }
}
